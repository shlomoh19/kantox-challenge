apiVersion: v1
kind: Service
metadata:
  name: main-api
  namespace: main-api
  labels:
    app: main-api
spec:
  type: NodePort
  selector:
    app: main-api
  ports:
  - name: http
    port: 80
    targetPort: 3000
    nodePort: 30080